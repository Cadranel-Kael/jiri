<div>
    <label for="{{ $name }}" class="font-bold">{{ $label }}</label>
    <input class="border-b-2 border-0" placeholder="{{ $placeholder ?? $label }}" type="text" name="{{ $name }}"
           id="{{ $name }}">
</div>
